apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: moodle-namespace
  labels:
    app: db
spec:
  ports:
  - protocol: TCP
    port: 3306
    targetPort: 3306
  selector:
    app: db
  clusterIP: None # Headless service for stateful apps
