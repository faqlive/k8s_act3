apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
  namespace: nodejs-mongo-namespace
  labels:
    app: nodejs
spec:
  type: LoadBalancer
  selector:
    app: nodejs
  ports:
  - protocol: TCP
    port: 30000
    targetPort: 3000
